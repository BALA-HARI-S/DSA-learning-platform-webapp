# NN — Topic Name

> Copy this file to `NN-topic/NN-descriptive-name.md` and fill in each section.
> Delete this quote block and any sections that don't apply.
> The goal is a *hybrid* lesson: a clear beginner-friendly core, plus depth for an
> experienced Java/Spring backend engineer.

## Overview

What this topic is, why it matters, and what you'll be able to do after the lesson.
2–4 sentences. State the prerequisite topics.

**You will learn to:**

- ...
- ...

---

## Core Concept

First-principles explanation. Assume the reader knows Java syntax but not this data
structure/algorithm. Use small, concrete examples and a diagram-in-text if helpful.

```java
// minimal illustrative snippet
```

---

## Java Implementation Notes

How this maps to real Java types and the runnable code for this lesson.

- Relevant JDK types: `...`
- Runnable code: [`code/src/main/java/dsa/<pkg>`](../../code/src/main/java/dsa)
- Run the tests: `cd code && mvn test -Dtest='dsa.<pkg>.*'`

---

## Worked Examples

For each example: the code, a short trace, and the complexity.

### Example 1: ...

```java
```

```text
Time:  O(...)
Space: O(...)
```

---

## Complexity Summary

| Operation / pattern | Time | Space | Notes |
|---|---:|---:|---|
| ... | `O(?)` | `O(?)` | ... |

---

## For Experienced Devs

Go deeper than the basics:

- JDK Collections internals (backing structure, resizing, load factor, etc.)
- Memory layout / boxing / cache behavior where relevant
- Common gotchas and "looks O(1) but isn't" traps
- API choices: which standard class to actually reach for, and when not to

---

## Interview & Backend Notes

- **Patterns**: the reusable techniques this topic unlocks (two-pointer, sliding
  window, fast/slow, hashing for O(1) lookup, etc.)
- **Backend relevance**: where this shows up in real Java/Spring work (request
  handling, batching DB results, pagination, caching, validation, streaming).
- **Red flags**: signals in a problem statement that hint this technique applies.

---

## Practice Problems

Solve these, then add your solution as a method + test under the lesson's package.

1. ... — LeetCode / HackerRank link
2. ...
3. ...

---

## Quiz

### Q1

```java
```

```text
Time: ?
Space: ?
```

---

## Answer Key

### Q1 Answer

```text
Time: O(?)
Space: O(?)
```

Explanation: ...

---

## Next Steps

What to study next and the suggested continue prompt.

```text
Continue my DSA learning with <next topic> and document it in the DSA folder.
```
